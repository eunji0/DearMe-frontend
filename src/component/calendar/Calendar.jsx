import React from 'react';

//  캘린더 컴포넌트
const Diary = ({ diaryData }) => {


  return (
    <div>
      {/* {diaryData.map((item, index) => (
        <div key={index}>
          <p>ID: {item.id}</p>
          <p>Username: {item.username}</p>
          <p>CoordinateX: {item.coordinateX}</p>
          <p>CoordinateY: {item.coordinateY}</p>
          <p>ImageType: {item.imageType}</p>
        </div>
      ))} */}
    </div>
  );
};

export default Diary;
